1. app.all()

app.all() handles ALL HTTP methods (GET, POST, PUT, DELETE, PATCH, etc.) for a specific route.

✔ Useful for:

Logging

Authentication middleware

Handling a route for every method


* is wildcard

we can use combination of 
app.all(*)----to design invalid page


if we are submitting form data with GET then we will get it in query param